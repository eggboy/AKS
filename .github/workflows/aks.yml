on:
  push:
    branches: [ "main" ]

permissions:
  id-token: write
  contents: read
  
jobs:
  kubelogin:
    runs-on: self-hosted
    defaults:
      run:
        shell: bash
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
      - run: |
          ls -la
      - name: Use local my-action
        uses: ./GithubAction/kubelogin.yml
